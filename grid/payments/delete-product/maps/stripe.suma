profile = "payments/delete-product@1.0"
provider = "stripe"

map DeleteProduct {
  http DELETE "/products/{input.id}" {
    security "bearer"
    
    response 200 "application/json" {
    }

    response "application/json" {
      map error {
        title = body.error.message
      }
    }
  }
}
