profile = "chat/get-messages@1.0"
provider = "mock"

"""
GetMessages map
"""
map GetMessages {
    return map error if(input.destination === 'not-existing-dest') {
        title = 'channel_not_found'
    }

    map result {
        messages = [
            {
                id: '1',
                author: 'U01',
                text: 'test'
            }
        ]
    }
}
