profile = "social-media/upload-from-url@1.0"
provider = "mock"

map RegisterUpload {
  return map result {
    assetId = '125682506616099'
  }
}

map GetUploadState {
  return map result {
      state = 'Finished'
  }
}