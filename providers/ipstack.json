{
  "name": "ipstack",
  "services": [
    {
      "id": "default",
      "baseUrl": "{HTTP_SCHEME}://api.ipstack.com/"
    }
  ],
  "defaultService": "default",
  "securitySchemes": [
    {
      "id": "apikey",
      "type": "apiKey",
      "in": "query",
      "name": "access_key"
    }
  ],
  "parameters": [
    {
      "name": "HTTP_SCHEME",
      "description": "HTTPS or HTTP scheme. The scheme identifies the protocol to be used to access the IP Stack API. Only HTTP scheme is allowed in case of IP Stack FREE subscription."
    }
  ]
}
