profile = "communication/send-sms@1.0"
provider = "mock"

map SendMessage {
  map result {
    messageId: "1234"
  }
}

map RetrieveMessageStatus {
  map result {
    deliveryStatus: "accepted"
  }
}
